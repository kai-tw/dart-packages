.dart_tool/
.packages
build/
doc/api/

# Written by `flutter pub get` for Flutter-SDK members (kai_tw_ui_kit).
.flutter-plugins-dependencies

# Libraries do not pin transitive versions for their consumers, so the lock
# file is noise here. The workspace root would otherwise commit one lockfile
# covering every member.
pubspec.lock

.DS_Store

# `flutter test --coverage` output. Regenerated on demand, never reviewed.
coverage/

# plan-cycle per-repo ledger + plan store. Session state, not source.
.claude/.plan-cycle/
