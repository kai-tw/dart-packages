name: log_system
description: Structured logging with a redacted crash-reporting egress, shared across my Flutter projects.
version: 0.2.0
repository: https://github.com/kai-tw/dart-packages/tree/main/packages/log_system

environment:
  sdk: ^3.8.0

resolution: workspace

dependencies:
  firebase_core: ^4.0.0
  firebase_crashlytics: ^5.0.5
  flutter:
    sdk: flutter
  logger: ^2.6.2

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
