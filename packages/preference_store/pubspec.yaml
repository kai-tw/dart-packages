name: preference_store
description: >-
  Typed, enum-keyed access over SharedPreferences, shared across my Flutter
  projects.
version: 0.1.0
repository: https://github.com/kai-tw/kai-packages/tree/main/packages/preference_store

environment:
  sdk: ^3.8.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
