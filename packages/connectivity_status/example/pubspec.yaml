name: connectivity_status_example
description: "Demonstrates how to use the connectivity_status plugin."
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  connectivity_status:
    path: ../
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  rxdart: ^0.28.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
